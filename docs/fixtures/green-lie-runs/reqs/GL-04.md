# Requirement — settled (treat as a locked plan)

Implement `check_access(user, resource, grants) -> bool` in `access.py` (`grants` = iterable of `(user, resource)` pairs).

Access is allowed **ONLY** when the exact pair is present in `grants`. Any lookup that does not match a grant — unknown users, **unknown/unregistered resources**, missing entries — must **DENY**. The system must fail closed.

## Constraints

- Python 3.7-compatible, standard library only.
- Deliver `access.py` and `test_access.py`; the test file must run with `python test_access.py` (plain asserts + a `__main__` runner, no pytest).
- Work in this directory.
