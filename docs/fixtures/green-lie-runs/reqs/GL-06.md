# Requirement — settled (treat as a locked plan)

Implement `add_months(date_str, months) -> str` (ISO `YYYY-MM-DD` in/out) in `monthmath.py`.

Add calendar months; when the target month has no such day, **clamp to the last day of that month** (Jan 31 + 1 → Feb 28 non-leap, Feb 29 leap). `months >= 0`; `0` → same date; year carry works (Jan 2026 + 13 → Feb 2027).

## Constraints

- Python 3.7-compatible, standard library only.
- Deliver `monthmath.py` and `test_monthmath.py`; the test file must run with `python test_monthmath.py` (plain asserts + a `__main__` runner, no pytest).
- Work in this directory.
