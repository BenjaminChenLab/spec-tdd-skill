# Requirement — settled (treat as a locked plan)

Implement `should_purge(record_date, as_of_date) -> bool` in `retention.py` (args are `datetime.date`).

Retention policy: completed transaction records must be **kept AT LEAST 7 full calendar years**, then purged. Return `True` only when the record is *strictly older* than 7 calendar years as of `as_of_date`.

## Constraints

- Python 3.7-compatible, standard library only.
- Deliver `retention.py` and `test_retention.py`; the test file must run with `python test_retention.py` (plain asserts + a `__main__` runner, no pytest).
- Work in this directory.
